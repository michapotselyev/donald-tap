import styles from "./style.module.scss";

const Tap = () => {
  return (
    <section className={styles.container}>
      <button className={styles.tap}></button>
    </section>
  );
};

export default Tap;
